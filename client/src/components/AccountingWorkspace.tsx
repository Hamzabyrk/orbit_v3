/* MoneyFlow style reminder: accounting administration remains in the same light operational system—thin rules, precise tables, cool neutrals and #2563EB only for intentional actions. */
import { useState } from "react";
import { Archive, ArrowLeft, BarChart3, Banknote, Building2, Calculator, ChevronDown, FileChartColumn, FileText, Landmark, LayoutDashboard, Menu, ReceiptText, Settings, ShoppingCart, Users, X } from "lucide-react";
import { toast } from "sonner";
import { ArchiveScreen, ChartAccountsScreen, LedgerScreen } from "@/components/AccountingModules";
import { SettingsPanel, type CompanySettings } from "@/components/SettingsPanel";

type Customer = { id: string; name: string };
type Supplier = { id: string; name: string };
type Invoice = { id: string; number: string; customerId: string; title: string; amount: number; issueDate: string; status: string };
type Expense = { id: string; number: string; supplierId: string; title: string; category: string; amount: number; expenseDate: string; status: string };
type BankAccount = { id: string; name: string; ledgerBalance: number };
type BankTransaction = { id: string; accountId: string; date: string; description: string; direction: "Gelen" | "Giden"; amount: number; category: string; status: string; reference?: string };
type JournalLine = { id: string; accountCode: string; accountName: string; debit: number; credit: number };
type JournalEntry = { id: string; number: string; date: string; description: string; source: "Fatura" | "Gider" | "Banka" | "Manuel"; reference?: string; status: "İşlendi" | "Taslak"; lines: JournalLine[] };
type AccountType = "Varlık" | "Yükümlülük" | "Gelir" | "Gider" | "Özkaynak";
type ChartAccount = { id: string; code: string; name: string; type: AccountType; group: string; normalBalance: "Borç" | "Alacak"; active: boolean };
type ArchiveItem = { id: string; type: "Fatura" | "Gider" | "Satış" | "Banka"; reference: string; title: string; date: string; amount: number; reason: string; source: "Faturalar" | "Giderler" | "Satışlar" | "Bankalar"; archived: boolean };
type AccountingData = { customers: Customer[]; suppliers: Supplier[]; invoices: Invoice[]; expenses: Expense[]; bankAccounts: BankAccount[]; bankTransactions: BankTransaction[]; journalEntries: JournalEntry[]; chartAccounts: ChartAccount[]; archiveItems: ArchiveItem[]; settings: CompanySettings };
type AccountingSection = "Muhasebe" | "Hesap Planı" | "Arşiv" | "Ayarlar";
type FinanceSection = "Bankalar" | "Satışlar" | "Raporlar";
type MainSection = "Dashboard" | "Müşteriler" | "Faturalar" | "Giderler" | "Tedarikçiler";

export function AccountingWorkspace({ data, initialSection, onMainNavigate, onFinanceNavigate, onCreateJournal, onCreateAccount, onRestoreArchive, onSaveSettings, onReset }: { data: AccountingData; initialSection: AccountingSection; onMainNavigate: (section: MainSection) => void; onFinanceNavigate: (section: FinanceSection) => void; onCreateJournal: (entry: JournalEntry) => void; onCreateAccount: (account: ChartAccount) => void; onRestoreArchive: (item: ArchiveItem) => void; onSaveSettings: (settings: CompanySettings) => void; onReset: () => void }) {
  const [active, setActive] = useState<AccountingSection>(initialSection); const [mobileOpen, setMobileOpen] = useState(false);
  const operationalNav = [{ label: "Dashboard", icon: LayoutDashboard }, { label: "Faturalar", icon: FileText }, { label: "Giderler", icon: ReceiptText }, { label: "Müşteriler", icon: Users }, { label: "Tedarikçiler", icon: Building2 }, { label: "Bankalar", icon: Banknote }, { label: "Satışlar", icon: ShoppingCart }, { label: "Raporlar", icon: BarChart3 }];
  const accountingNav = [{ label: "Muhasebe", icon: Calculator }, { label: "Hesap Planı", icon: FileChartColumn }, { label: "Arşiv", icon: Archive }, { label: "Ayarlar", icon: Settings }];
  const navigate = (label: string) => { if (label === "Muhasebe" || label === "Hesap Planı" || label === "Arşiv" || label === "Ayarlar") { setActive(label); setMobileOpen(false); return; } if (label === "Bankalar" || label === "Satışlar" || label === "Raporlar") { onFinanceNavigate(label); return; } onMainNavigate(label as MainSection); };
  const openSource = (source: ArchiveItem["source"]) => navigate(source);
  return <div className="dashboard-shell min-h-screen bg-[#f8fafc] text-slate-900 lg:flex"><button aria-label="Menüyü kapat" onClick={() => setMobileOpen(false)} className={`fixed inset-0 z-30 bg-slate-900/25 lg:hidden ${mobileOpen ? "block" : "hidden"}`} /><aside className={`fixed inset-y-0 left-0 z-40 flex w-[252px] flex-col border-r border-slate-200 bg-white px-4 py-5 shadow-xl transition-transform lg:static lg:shrink-0 lg:translate-x-0 lg:shadow-none ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="mb-8 flex items-center justify-between px-1"><div className="flex items-center gap-2.5"><span className="grid h-8 w-8 place-items-center rounded-lg bg-blue-600 text-white"><Calculator className="h-4 w-4" /></span><span className="font-display text-[18px] font-extrabold tracking-[-0.05em] text-slate-900">MoneyFlow</span></div><button onClick={() => setMobileOpen(false)} className="grid h-8 w-8 place-items-center rounded-md text-slate-400 hover:bg-slate-100 lg:hidden"><X className="h-4 w-4" /></button></div><nav className="space-y-1">{operationalNav.map((item) => { const Icon = item.icon; return <button key={item.label} onClick={() => navigate(item.label)} className="flex h-10 w-full items-center gap-3 rounded-lg px-3 text-left text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50 hover:text-slate-900"><Icon className="h-[17px] w-[17px] text-slate-400" /><span>{item.label}</span></button>; })}</nav><div className="my-5 border-t border-slate-100" /><nav className="space-y-1">{accountingNav.map((item) => { const Icon = item.icon; const selected = active === item.label; return <button key={item.label} onClick={() => navigate(item.label)} className={`relative flex h-10 w-full items-center gap-3 rounded-lg px-3 text-left text-[13px] font-semibold transition ${selected ? "bg-blue-50 text-blue-700" : "text-slate-600 hover:bg-slate-50 hover:text-slate-900"}`}>{selected && <span className="absolute -left-[17px] h-6 w-1 rounded-r bg-blue-600" />}<Icon className={`h-[17px] w-[17px] ${selected ? "text-blue-600" : "text-slate-400"}`} /><span>{item.label}</span></button>; })}</nav><div className="mt-auto border-t border-slate-100 pt-4"><button onClick={() => onMainNavigate("Dashboard")} className="flex h-10 w-full items-center gap-3 rounded-lg px-3 text-[13px] font-semibold text-slate-600 hover:bg-slate-50"><ArrowLeft className="h-[17px] w-[17px] text-slate-400" />Ana çalışma alanı</button></div></aside><main className="min-h-screen flex-1"><header className="sticky top-0 z-20 flex h-[72px] items-center justify-between border-b border-slate-200 bg-white/95 px-4 backdrop-blur sm:px-6 lg:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="grid h-9 w-9 place-items-center rounded-lg text-slate-600 hover:bg-slate-100 lg:hidden"><Menu className="h-5 w-5" /></button><div><p className="text-[12px] font-medium text-slate-400">MoneyFlow / Muhasebe Yönetimi</p><h1 className="font-display text-[18px] font-extrabold tracking-[-0.035em] text-slate-900">{active}</h1></div></div><button onClick={() => toast.info("Muhasebe çalışma alanı", { description: "Fişler, hesap planı, arşiv ve ayarlar aynı finansal bağlamda çalışır." })} className="flex items-center gap-2 rounded-lg px-1.5 py-1 hover:bg-slate-100"><span className="grid h-8 w-8 place-items-center rounded-full bg-blue-100 text-[11px] font-bold text-blue-700">DK</span><span className="hidden text-left sm:block"><span className="block text-[12px] font-bold text-slate-700">Demo Kullanıcı</span><span className="block text-[10px] text-slate-400">Muhasebe görünümü</span></span><ChevronDown className="hidden h-3.5 w-3.5 text-slate-400 sm:block" /></button></header>{active === "Muhasebe" && <LedgerScreen data={data} onCreate={onCreateJournal} />}{active === "Hesap Planı" && <ChartAccountsScreen data={data} onCreate={onCreateAccount} />}{active === "Arşiv" && <ArchiveScreen data={data} onRestore={onRestoreArchive} onOpenSource={openSource} />}{active === "Ayarlar" && <SettingsPanel settings={data.settings} onSave={onSaveSettings} onReset={onReset} />}</main></div>;
}
